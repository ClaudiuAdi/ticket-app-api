const body = require('./body');
const contactFormBody = require('./contact-form-body');
const footer = require('./footer');
const header = require('./header');
const newTicketBody = require('./new-ticket-body');
const platformLink = require('./platform-link');

module.exports = {
  body,
  contactFormBody,
  platformLink,
  footer,
  newTicketBody,
  header,
};
